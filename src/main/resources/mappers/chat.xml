<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chat">

    <insert id="addChat" parameterType="chatLogDto">
    	insert into CHAT_LOG (SEQ, CONTENT, REGDATE, TYPE, CHAT_MEMBER_SEQ, CHAT_ROOM_SEQ) 
    	values (CHAT_LOG_SEQ.nextval, #{content}, SYSTIMESTAMP AT TIME ZONE 'Asia/Seoul', #{type}, #{chatMemberSeq}, #{chatRoomSeq})
    </insert>

</mapper>