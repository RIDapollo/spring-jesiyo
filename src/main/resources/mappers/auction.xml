<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="auction">

    <select id="list" parameterType="map" resultType="AuctionDto">
        SELECT * FROM (
            SELECT 
                s.*, 
                ROWNUM as rnum 
            FROM (
                SELECT * FROM auction
                
                <where>
                    <if test="word != null and word != ''">
                        AND (name LIKE '%' || #{word} || '%' OR description LIKE '%' || #{word} || '%')
                    </if>
                </where>
                
                ORDER BY seq DESC
            ) s
        ) 
        WHERE rnum BETWEEN #{begin} AND #{end}
    </select>
    
    <select id="getTotalCount" parameterType="map" resultType="int">
        SELECT count(*) 
        FROM auction
        <where>
            <if test="word != null and word != ''">
                AND (name LIKE '%' || #{word} || '%' OR description LIKE '%' || #{word} || '%')
            </if>
        </where>
    </select>
    
    <select id="getMdto" parameterType="int" resultType="com.test.jesiyo.auction.dto.MemberDto">
        select * from member where seq = #{_parameter}
    </select>
    
    <insert id="addMaster" parameterType="map">
        <selectKey keyProperty="dto.seq" resultType="int" order="BEFORE">
            SELECT auction_master_seq.nextval FROM dual
        </selectKey>
        INSERT INTO auction_master (seq, type)
        VALUES (#{dto.seq}, '0')
    </insert>

    <insert id="addAuction" parameterType="map">
        INSERT INTO auction (
            seq, 
            name, 
            bid_open_price, 
            image, 
            end_date, 
            description, 
            created_at, 
            status, 
            winner_seq, 
            create_member_seq, 
            category_seq
        ) VALUES (
            #{dto.seq},           #{dto.name}, 
            #{dto.bidOpenPrice},  #{dto.image}, 
            TO_DATE(#{dto.endDate}, 'YYYY-MM-DD"T"HH24:MI'), 
            #{dto.description}, 
            default, 
            '0', 
            null, 
            #{mdto.seq}, 
            #{dto.categorySeq}
        )
    </insert>
    
    <select id="getDetail" parameterType="int" resultType="AuctionDto">
        select * from auction where seq = #{_parameter}
    </select>
    
    <select id="getHighestBid" parameterType="int" resultType="AuctionDto">
        select a.*,
           (select max(bid_price)
            from bid_history b
            where b.auction_seq = a.seq) as highestBid
        from auction a
        where a.seq = #{_parameter}
    </select>
    
    <insert id="bid" parameterType="map">
        insert into bid_history (seq, bid_price, bid_time, status, member_seq, auction_seq) 
            values (bid_history_seq.nextVal, #{bidPrice}, default, 0, #{memberSeq}, #{seq})
    </insert>
    
    <select id="getLatestBids" parameterType="int" resultType="BidDto">
    	SELECT *
		FROM bid_history
		WHERE auction_seq = #{_parameter}
		ORDER BY bid_time DESC
		FETCH FIRST 5 ROWS ONLY
    </select>

</mapper>