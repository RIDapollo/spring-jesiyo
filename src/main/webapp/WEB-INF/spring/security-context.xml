<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <beans:bean id="bcryptEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/member/login" access="permitAll" />
        <intercept-url pattern="/member/regist" access="permitAll" />
        <intercept-url pattern="/index" access="permitAll" />
        <intercept-url pattern="/resources/**" access="permitAll" />
        
        <intercept-url pattern="/member/mypage" access="hasRole('ROLE_USER')" />
        
        <intercept-url pattern="/direct-sales/*/edit" access="isAuthenticated()" />
        <intercept-url pattern="/direct-sales" method="POST" access="isAuthenticated()" />

        <form-login 
            login-page="/member/login" 
            login-processing-url="/member/login" 
            authentication-success-handler-ref="customSuccessHandler"
            authentication-failure-url="/member/login?error=true"
            username-parameter="userId"
            password-parameter="userPw" />

        <logout 
            logout-url="/member/logout" 
            logout-success-url="/index?msg=logout" 
            invalidate-session="true" />
            
        <csrf disabled="true"/>
        
        <!-- iframe을 위한 시큐리티 설정 -->
        <headers>
	        <frame-options policy="SAMEORIGIN" />
	    </headers>
        
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder ref="bcryptEncoder" />
        </authentication-provider>
    </authentication-manager>
	<beans:bean id="customSuccessHandler" class="com.test.jesiyo.member.handler.CustomSuccessHandler" />
	<beans:bean id="customUserDetailsService" class="com.test.jesiyo.member.service.CustomUserDetailsService" />
</beans:beans>